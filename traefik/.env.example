# Traefik Configuration
# Copy this file to .env and fill in your actual values

# Domain Configuration
DOMAIN=example.com  # Your root domain

# Let's Encrypt Configuration
ACME_EMAIL=admin@example.com  # Email for Let's Encrypt certificates

# Cloudflare API (for DNS Challenge)
CF_API_EMAIL=your-cloudflare-email@example.com  # Cloudflare account email
CF_DNS_API_TOKEN=your-cloudflare-api-token  # Cloudflare API token with DNS edit permissions

# Timezone
TZ=UTC  # Your timezone (e.g., Europe/Berlin, America/New_York)

# Traefik Dashboard Authentication (if using basic auth)
# Generate with: echo $(htpasswd -nb username password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_DASHBOARD_USERS=admin:$$2y$$10$$...  # Replace with generated hash